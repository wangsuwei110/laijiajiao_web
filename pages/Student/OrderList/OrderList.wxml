<!--pages/Student/OrderList/OrderList.wxml-->

<Empty wx:if='{{loaded && !orderList.length}}'/>
<block wx:else>
    <OrderListItem wx:for='{{orderList}}' item='{{item}}' wx:key='{{item.sid}}'/>
    <DropDown wx:if='{{loaded}}' isEnd='{{isEnd}}'/>
</block>